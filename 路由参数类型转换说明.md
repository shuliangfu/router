# 路由参数类型转换说明

## 问题

路由参数类型转换（string -> number）是否需要？

## 什么是路由参数类型转换？

### 当前情况

在 HTTP 路由中，URL 路径参数从 URL 中提取出来时，**都是字符串类型**。

例如：
- 路由：`/user/:id`
- 请求：`/user/123`
- router 返回：`params.id = "123"`（**字符串**）

### 类型转换的含义

类型转换是指：**自动将字符串参数转换为对应的类型**（如数字、布尔值等）。

例如：
- 路由：`/user/:id`
- 请求：`/user/123`
- 转换后：`params.id = 123`（**数字**）

## 示例对比

### 不使用类型转换（当前 router 库的行为）

```typescript
// 路由：/user/:id
// 请求：/user/123

const match = await router.match("/user/123");
console.log(match.params.id);        // "123" (字符串)
console.log(typeof match.params.id); // "string"

// 使用时需要手动转换
const userId = parseInt(match.params.id, 10); // 123 (数字)
```

### 使用类型转换（可选功能）

```typescript
// 路由：/user/:id
// 请求：/user/123

const match = await router.match("/user/123");
console.log(match.params.id);        // 123 (数字，自动转换)
console.log(typeof match.params.id); // "number"

// 直接使用，无需手动转换
const userId = match.params.id; // 123 (数字)
```

## 实现方式

### 方式 1：通过路由定义声明类型

```typescript
// 在路由定义中声明参数类型
const routes = [
  { path: "/user/:id(number)", handler: ... },  // id 自动转换为数字
  { path: "/post/:id(string)", handler: ... },  // id 保持字符串
  { path: "/flag/:enabled(boolean)", handler: ... }, // enabled 转换为布尔值
];
```

### 方式 2：通过类型定义自动推断

```typescript
// 在 handler 函数签名中定义类型
app.get("/user/:id", async (ctx: HttpContext<{ id: number }>) => {
  // ctx.params.id 自动是 number 类型
  const userId = ctx.params.id; // 123 (数字)
});
```

### 方式 3：在 HTTP 服务器层自动转换

```typescript
// HTTP 服务器自动检测并转换
// 如果参数看起来像数字，自动转换为数字
// /user/123 → params.id = 123 (number)
// /user/abc → params.id = "abc" (string，无法转换)
```

## 优缺点分析

### 优点

- ✅ **方便使用**：不需要手动 `parseInt()` 或 `Number()`
- ✅ **类型安全**：TypeScript 类型更准确
- ✅ **减少错误**：避免忘记转换导致的类型错误

### 缺点

- ❌ **增加复杂度**：需要实现类型检测和转换逻辑
- ❌ **可能误判**：自动转换可能不符合预期（如 `"123abc"` 应该转换吗？）
- ❌ **性能开销**：每次路由匹配都需要类型检测和转换
- ❌ **不够灵活**：某些场景下可能需要保持字符串类型

## 当前 router 库的行为

查看 `@dreamer/router` 的实现：

```typescript
// router/src/mod.ts
export interface RouteMatch {
  params: Record<string, string>;  // 所有参数都是字符串
  query: Record<string, string>;    // 查询参数也是字符串
}
```

**当前 router 库不进行类型转换**，所有参数都是字符串。

## 建议

### 推荐：不实现自动类型转换

**理由**：
1. **保持简单**：router 库已经返回字符串，HTTP 服务器不需要额外处理
2. **灵活性**：用户可以根据需要手动转换，更灵活
3. **明确性**：字符串类型更明确，不会有意外的类型转换
4. **性能**：避免不必要的类型检测和转换开销

### 如果用户需要类型转换

可以在 handler 中手动转换：

```typescript
app.useRouter(router);

// 在 handler 中手动转换
const match = await router.match(request.url.pathname, { method: request.method });
if (match && match.isApi) {
  const handler = match.handlers[request.method];
  if (handler) {
    // 手动转换参数类型
    const params = {
      ...match.params,
      id: parseInt(match.params.id, 10), // 转换为数字
    };

    const response = await handler(request, {
      params,
      query: match.query,
    });
    return response;
  }
}
```

或者提供一个辅助函数：

```typescript
// 辅助函数：转换参数类型
function convertParams(params: Record<string, string>, schema: Record<string, "number" | "string" | "boolean">) {
  const converted: Record<string, unknown> = {};
  for (const [key, type] of Object.entries(schema)) {
    const value = params[key];
    if (value === undefined) continue;

    switch (type) {
      case "number":
        converted[key] = Number(value);
        break;
      case "boolean":
        converted[key] = value === "true" || value === "1";
        break;
      default:
        converted[key] = value;
    }
  }
  return converted;
}

// 使用
const params = convertParams(match.params, { id: "number" });
```

## 结论

**建议：不实现自动类型转换**

- router 库已经返回字符串，保持一致性
- 用户可以根据需要手动转换，更灵活
- 避免增加复杂度和性能开销

如果需要，可以提供辅助函数或中间件来帮助用户进行类型转换。
